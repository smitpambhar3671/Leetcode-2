class Solution {
public:
    string add(string a, string b) {
        string ans = "";
        int i = a.size() - 1;
        int j = b.size() - 1;
        int carry = 0;

        while (i >= 0 || j >= 0 || carry) {
            int sum = carry;

            if (i >= 0)
                sum += a[i--] - '0';

            if (j >= 0)
                sum += b[j--] - '0';

            ans += char(sum % 10 + '0');
            carry = sum / 10;
        }

        reverse(ans.begin(), ans.end());
        return ans;
    }

    bool check(string& num, int pos, string a, string b, int count) {
        if (pos == num.size())
            return count >= 3;

        string sum = add(a, b);

        if (num.substr(pos, sum.size()) != sum)
            return false;

        return check(num, pos + sum.size(), b, sum, count + 1);
    }

    bool isAdditiveNumber(string num) {
        int n = num.size();

        for (int i = 1; i < n; i++) {
            if (i > 1 && num[0] == '0')
                break;

            string a = num.substr(0, i);

            for (int j = i + 1; j < n; j++) {
                if (j - i > 1 && num[i] == '0')
                    break;

                string b = num.substr(i, j - i);

                if (check(num, j, a, b, 2))
                    return true;
            }
        }

        return false;
    }
};
